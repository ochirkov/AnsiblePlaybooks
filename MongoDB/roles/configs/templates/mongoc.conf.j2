#where to log
logpath=/var/log/mongodb/mongod.log

logappend=true

#bind_ip=127.0.0.1

# fork and run in background
fork = true

port = {{ configs_port }}

dbpath={{ data_path }}configdb

configsvr=true